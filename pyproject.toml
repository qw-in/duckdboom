[project]
name = "duckdboom"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.37.8",
    "duckdb>=1.2.1",
    "pyarrow>=19.0.1",
]
